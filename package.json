{
  "name": "gemyai",
  "private": true,
  "packageManager": "pnpm@10.6.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "start": "cd apps/server/app && uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
